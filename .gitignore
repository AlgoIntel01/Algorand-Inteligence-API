node_modules/
dist/
/data/
.env
# Any .env variant (backups, .env.local, …) carries the same secrets as .env.
# The example file is the one intentional exception.
.env.*
!.env.example
# Wallet mnemonics generated by the funding rail — never commit these.
*.mnemonic
*.log
.DS_Store

# Private strategy notes — not for the public repo
docs/ANNOUNCE.md
DEPLOYING.md
